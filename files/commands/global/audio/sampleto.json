{
    "name": "sampleto",
    "description": "Similar to sample, instead sampleto only plays the current track until a specific timestamp.",
    "options": [
        {
            "name": "timestamp",
            "description": "Timestamp at which to automatically skip the current playing audio. For example, 'sampleto 2m' will play the current track until it reaches 2:00 in.",
            "type": 3,
            "required": true
        }
    ]
}